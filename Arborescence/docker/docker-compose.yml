version: '3.8'

services:
  consul:
    image: hashicorp/consul:1.15
    container_name: consul-server
    restart: always
    ports:
      - "8500:8500" # Interface Web (UI) et API HTTP
      - "8600:8600/udp" # Serveur DNS interne
    command: "agent -dev -client=0.0.0.0"